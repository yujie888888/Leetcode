** Binaray Search **
    * Time Complexity *
        O(nlogn)
    * 使用场景 *
        array必须是sort好的
    * 模版 *
        int left = 0;
        int right = length-1;
        int mid;
        while(left <= right){
            mid = left + (eight-left) + 1;
            if(nums[mid] == target) return mid;
            else if(nums[mid] < target) left = mid + 1;
            else right = right - 1;
        }